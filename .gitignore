/target
tools/
kodex-out/
# caucus session state + sub-agent git worktrees (embedded repos): never commit.
.caucus/
# local review scratch + macOS finder cruft.
review/
.DS_Store
# trunk + build.rs populate assets/spa/ on every build (hashed wasm/js/css).
# The committed source-of-truth fallback lives in assets/spa-placeholder/.
/assets/spa/
