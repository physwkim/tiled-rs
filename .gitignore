/target
tools/
kodex-out/
# caucus session state + sub-agent git worktrees (embedded repos): never commit.
.caucus/
# local review scratch + macOS finder cruft.
review/
.DS_Store
# The SPA bundle is staged by build.rs into $OUT_DIR/spa/ (under target/),
# never the source tree, so there is nothing to ignore here. The committed
# source-of-truth fallback lives in assets/spa-placeholder/.
